# Archive

Older alternate layouts. Prefer the live packages at the repo root (`burst/`, `simple/`, etc.).

| Path | Role |
|------|------|
| `burst_probes_era/` | Flat pre-`burst.core` analysis modules |
| `burst_simple_snapshot/` | Snapshot of an earlier simple-pipeline layout |
